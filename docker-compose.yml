version: "3"
services:
  web:
    container_name: test-sales
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    build: 
      context: .
      dockerfile: Dockerfile
    ports: 
      - 2935:80

